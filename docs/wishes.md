# Wishes

## マルチテナント対応(設計検討分)

- [x] tenant_users, event_config, event_dataなどのRLS方針を決定(`is_member_of_tenant`関数を共通利用)
- [x] 1ユーザ複数テナント所属を許容する方針を決定(Slackは1ワークスペース1テナント固定)
- [x] テナント内のroleはフラットとする方針を決定し、`tenant_users.role`カラムは廃止
- [x] テナント切替はURLのslug方式で行う方針を決定(存在しないslug/未所属slugは同じ扱いにして探索を防ぐ)
- [x] 事前登録(email)→初回ログインでuser_id自動紐付け(`link_my_tenant_user`)の方式を決定
- [x] 役員一覧・slack2mail設定ファイル・イベント参加者一覧はSpreadsheetを原本とし、新旧比較ではなく全件同期方式(`sync_tenant_members`/`sync_event_configs`/`sync_event_data`)で取り込む方針を決定
- [x] メンバー追加時、他テナントで既にuser_idが確定していれば横展開する方式を決定
- [x] event_configから消えたイベントはevent_dataも道連れ削除する方針を決定(個人情報保護の観点、送達記録はsent_emailsに残るため実害なし)
- [x] Google Spreadsheet/Excelの読み込みはテナントごとの専用Service Account方式とする方針を決定(メンバーOAuth方式はExcel対応に伴うDrive readonlyスコープがRestricted Scopeに該当し審査負担が大きいため見送り)
- [x] Service Accountの秘密鍵はSupabase Vaultに保存し、同一性確認にはprivate_key_idの一部表示で対応する方針を決定
- [x] SQLマイグレーションへの反映(alter table, RLSポリシー, 関数群)
- [x] middleware(認可)・select-tenant画面の実装
- [ ] テナント作成の当面の運用手順をドキュメント化(手動SQL運用)

## 初期環境構築

- [x] slackにアプリを登録する(登録済みであるがslackへのアプリインストールのベストプラクティスを探す)
- [ ] GithubからCI/CDでデプロイできるようにする
  - [ ] Githubに環境変数を登録する
- [x] 仮想マシン上でDockerで稼働させる
  - [x] OCIを選定。実行環境を構築する。
- [x] Supabase 本番用を開設する
- [x] Supabase に初期テーブルを構築する(Create Table, RPC, ...)
- [x] Supabase に初期値をロードする
  - [x] reserved_slugsをseed.sqlからロードする方式とする
る

## 初期設定

※ 以下はシングルテナント時代の初期設定方式。マルチテナント化後は「マルチテナント対応(設計検討分)」セクションの方式に置き換わる(手動テナント作成＋事前登録＋初回ログイン時自動紐付け)。

- [x] 初期ユーザは初期設定でboardmembersに登録する(envファイル) ※旧方式
- [x] 起動時に初期ユーザでログインする
- [x] boradmembersは初期ユーザでロードする。ロードするシートの情報はenvから読み取る
- [x] boardmembersテーブルはboardmembersテーブルに登録されているemailのユーザのみアクセスできること
- [x] slack2mail_configを読み込みイベントのシートに関する設定を記憶する(テーブルへ読み込む)
- [x] slack2mail_confgのシートIDはenvから読み込む
- [x] slack2mail_configが読み込まれたあとにイベントのデータを登録する
- [x] イベントのデータはslakc2mail_configに指定されたシートから指定したカラムを読み込む
- [x] メール本文埋め込み用の値をslack2mail_configで指定できるようにする

## 初期設定Webインタフェース

- [x] イベントシートの追加をWebから行える
  - [x] イベントシートを選択
  - [x] シート内のカラム名とevent_configと紐付ける
  - [x] 登録ボタンで登録される
- [ ] メールの作成を途中からWebでも行える
  - [ ] いちどメッセージを投稿後、Webで編集ボタンを押下して呼び出す
  - [ ] WebUIが表示されるので編集し保存する

## メール作成オペレーション

- [x] /slack2mailコマンドでモーダルダイアログ表示
- [x] イベント選択でイベントに応じた宛先グループが表示される
- [x] 送信日時欄から送信日時を指定することで送信予約ができる。予約しない場合は確認後すみやかに、となる。
- [x] 保存ボタンでmailsに保存
- [x] 編集ボタンで同じ内容について編集
- [x] 中止ボタンで送信中止ステータスに
- [x] 確認ボタンで確認者追加
- [x] 確認者が規定数に達したところで承認済みへ
- [x] 本文中に宛先に応じた値を埋め込める(メール本文埋め込み用の値)
  - [x] slack2mail_configでカラムを指定する
  - [x] 埋め込み用のプレースホルダー '(((カラム名)))' (シングルクォートはここでの表現のみ。入力時は不要)
  - [x] プレースホルダーであることを識別する'((( )))'は全角も可とする
  - [x] 埋め込みできる文字がなにかわかるようにモーダルに表示しておく
  - [x] 埋め込みが正しく行われたか確認できるように埋め込み後のイメージを表示できるようにする

## メール作成(コピー新規)

- [x] Message Shortcut 「ここから新しいメールを作成」で投稿内容と同じ内容の新規メッセージ作成
- [x] モーダル表示後は新規と同様

## メール確認数の変更

- [x] 編集ボタンから修正
- [x] Message Shortcut 「メール確認数の変更」は本番デプロイまでに削除する

## メール送信

- [x] 保存されたメールが指定された時刻を過ぎたら送信される
- [x] メールは宛先ごとに1通ずつ作成される
- [x] メール送信時に宛先ごとに指定された値を埋め込む
- [x] 宛先ごとに確認用リンクを作成しメール本文に追記する
- [x] 受診者はリンクをクリックすることでメッセージを確認したことを通知できる
- [x] 送信したメッセージは送信者として指定したメールアドレスのIMAP sentフォルダに転記する

## メール受信確認

- [x] 受診者が本文内のリンクをクリックすることでメールを受信したことをシステムへ通知できる
- [x] 通知を受信するとSlackの投稿の受信確認数を現在値に更新する


## Tasks

- [x] system_settings : 不要。後で消す(昔はGoogleのリフレッシュトークンを保存していた。いまは方式変更済み)
- [x] slackからのリクエストを検証する(X-Slack-Signature,X-Slack-Request-Timestamp)
- [x] /slack2mailコマンドのオプション(件名)は指定できないように変更する
- [x] Webインタフェースのデザイン修正
- [x] イベントシートの追加をWebから行える
  - [x] イベントシートのURLを入力欄にコピー&ペースト → Sheet IDを取得
  - [x] シートの項目名が表示されるのでそこから選択していく
  - [x] 項目名は2行目の場合もあるので指定できるようにする
  - [x] UIを考える
  - [x] event_configテーブルに登録する
  - [x] slack2mail_confgシートに反映する
- [ ] Webで編集ボタンを設置
- [ ] リファクタリング
  - [ ] camelCase, snake_case, kebab-caseの混在を適切に整理
  - [ ] テストケースの修正・追加
  - [ ] 共通コードのutil化
  - [ ] TODOの消化
  - [ ] MarkdownのGithub対応 - "\<br\>"相当の改行のため空白文字を2つ追記する
- [ ] デプロイ準備
  - [ ] GithubへのPush
  - [ ] CI/CD作成
  - [x] OCI本番環境作成(テナントのマスターアカウント)
  - [x] Supabase本番環境作成(テナントのマスターアカウント)




Webで編集ボタン

```
{
  "type": "actions",
  "elements": [
    {
      "type": "button",
      "text": {
        "type": "plain_text",
        "text": "Webで編集"
      },
      "url": "https://example.com/mail/123/edit"
    }
  ]
}
```
